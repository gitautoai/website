import React from 'react';

const GettingStarted = () => (
  <section id="getting-started">
    <div className="container">
      <h2>How to Get Started</h2>
      <p>First, create an issue, such as a bug report or feature request. Label it as <code>gitauto</code>. Then, you just need to wait for the PR to be created. GitAuto updates you on progress in the issue comments.</p>
      <p>If possible, try to base this off of the <code>nikita</code> branch and not the <code>main</code> branch.</p>
    </div>
  </section>
);

export default GettingStarted;
